import pygame
import os
import sys
from os.path import join
from os import walk

HUD_HEIGHT = 50
MAP_WIDTH = 800
MAP_HEIGHT = 600

FPS = 60

WINDOW_WIDTH = MAP_WIDTH
WINDOW_HEIGHT = MAP_HEIGHT + HUD_HEIGHT

TILE_SIZE = 40
